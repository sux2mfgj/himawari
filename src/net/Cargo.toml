[package]
name = "net"
version = "0.1.0"
edition = "2021"

[dependencies]
# Meson ビルド時は bindings_net はリンクされる
# Cargo ビルド時は以下のモックを使用
mm = { path = "../../rust/mocks/mm" }
print-rs = { path = "../../rust/mocks/print-rs" }

# build.rs で bindgen を実行するための依存
[build-dependencies]
bindgen = "0.70"

# テスト時のみモックを使用
[dev-dependencies]
runtime-rs = { path = "../../rust/mocks/runtime-rs" }
