ENTRY(entry)

SECTIONS
{
    . = 0x100000;
    .text :{
        *(.entry);
        *(.text);
    }
    .data :{    /* 有意値で初期化されるstatic変数 */
        *(.data) 
    }
    .bss :{     /* ゼロで初期化されるstatic変数 */

        *(.bss);
    }

    end = .;
}
