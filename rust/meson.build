# Build Rust bindings and runtime libraries
subdir('bindings')
subdir('runtime')

# Note: C header generation from Rust is handled in the main meson.build
# after source files have populated the rs_hdr array
