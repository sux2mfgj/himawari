[package]
name = "runtime-rs"
version = "0.1.0"
edition = "2021"

# このクレートはテスト専用のモック
# 本物の runtime_rs と同じ名前だが、テスト時にのみ使用される

[dependencies]
# テスト環境では print-rs モックを使用
print-rs = { path = "../print-rs" }
